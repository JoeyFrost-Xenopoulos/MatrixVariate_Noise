% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Optimal_k_test.R
\name{run_optimal_k_test_scheme}
\alias{run_optimal_k_test_scheme}
\title{Extensive HC Noise-k Test Scheme}
\usage{
run_optimal_k_test_scheme(
  size_grid = default_optimal_k_size_grid(),
  n_grid = c(30L, 50L, 100L),
  contam_grid = c(0L, 5L, 10L, 20L),
  n_sim = 20L,
  g = 2L,
  signal_shift = 6,
  row_sd = 0.35,
  col_sd = 0.35,
  contam_low = -15,
  contam_high = 15,
  noise_k_grid = 10^seq(-150, -1, length.out = 25),
  max_iter = 50L,
  tol = 1e-06,
  nstart = 50L,
  verbose = FALSE,
  csv_path = NULL,
  seed = NULL
)
}
\arguments{
\item{size_grid}{A list of integer length-2 vectors giving the matrix
dimensions to test. The default covers a broad progression of shapes from
very small to substantially larger matrices.}

\item{n_grid}{Integer vector of sample sizes to test for each matrix shape.}

\item{contam_grid}{Integer vector of contamination counts to inject into
each simulated dataset.}

\item{n_sim}{Integer: number of replicated simulations for each scenario.}

\item{g}{Integer: number of Gaussian mixture components.}

\item{signal_shift}{Numeric: separation between the clean Gaussian component
means.}

\item{row_sd}{Numeric: standard deviation scale for the row-side simulation.}

\item{col_sd}{Numeric: standard deviation scale for the column-side
simulation.}

\item{contam_low}{Numeric: lower bound for contaminated matrix entries.}

\item{contam_high}{Numeric: upper bound for contaminated matrix entries.}

\item{noise_k_grid}{Numeric vector passed to \code{matrix_variate_noise_fit()} when
\code{select_noise_k = TRUE}.}

\item{max_iter}{Integer: maximum EM iterations.}

\item{tol}{Numeric: EM convergence tolerance.}

\item{nstart}{Integer: number of k-means restarts for initialization.}

\item{verbose}{Logical: print progress messages.}

\item{csv_path}{Optional path or filename stem for a CSV file to write the
per-simulation results at the end of the run.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A list with three elements:
\itemize{
\item \code{results}: one row per simulation replicate and scenario.
\item \code{summary}: scenario-level summaries of the selected \code{k} values.
\item \code{scaling_fit}: a simple regression fit that helps inspect whether the
selected \code{k} is scaling with dimension, sample size, or contamination.
}
}
\description{
This file defines a simulation harness for studying how the automatically
selected HC noise height changes with matrix size, sample size, and the
amount of contamination.
}
\details{
The goal is to make the search for an optimal \code{k} more empirical: generate
many matrix-valued datasets of different shapes, contaminate some of the
observations with outliers, fit the matrix-variate noise mixture with
\code{select_noise_k = TRUE}, and then summarize the selected \code{k} values in a way
that makes size-scaling patterns easy to inspect.
}
\examples{
\dontrun{
run_optimal_k_test_scheme(
  size_grid = default_optimal_k_size_grid(),
  n_grid = c(30, 60),
  contam_grid = c(0, 5, 10),
  n_sim = 5,
  csv_path = "output/optimal_k_results.csv",
  seed = 1
)
}

}
