% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Diagnostics.R
\name{diagnose_kmeans_wcss}
\alias{diagnose_kmeans_wcss}
\title{Diagnose whether k-means WCSS is better/worse than true classes}
\usage{
diagnose_kmeans_wcss(
  data,
  true_labels = NULL,
  kmeans_labels = NULL,
  g = NULL,
  nstart = 10,
  verbose = TRUE
)
}
\arguments{
\item{data}{Either a list of matrices (all same dimensions) or a numeric matrix
where each row is an observation (already vectorised).}

\item{true_labels}{An integer vector of true class labels (1..K). Can be NULL.}

\item{kmeans_labels}{An integer vector of cluster assignments from k-means (1..g).
If NULL, k-means is run internally with \code{nstart} restarts.}

\item{g}{Integer: number of clusters for k-means (only needed if \code{kmeans_labels} is NULL).}

\item{nstart}{Integer: number of restarts for k-means (default 10).}

\item{verbose}{Logical: if TRUE, prints the diagnostic message.}
}
\value{
Invisibly a list with:
\item{A}{WCSS of the k-means solution}
\item{B}{WCSS of the true classes (NA if not provided)}
\item{recommendation}{A character string describing the outcome}
}
\description{
Diagnose whether k-means WCSS is better/worse than true classes
}
\details{
WCSS is the total within-cluster sum of squares (sum of squared Euclidean
distances of points to their cluster centroid).
\itemize{
\item If \code{B < A}, the true classes have lower WCSS → a better clustering
exists. The k-means solution is likely stuck in a local optimum, so
a smarter initialisation (e.g. k-means++ or increasing \code{nstart}) may help.
\item If \code{A < B}, k-means found a tighter Euclidean clustering than the true
labels. This usually means the true structure is not well captured by
Euclidean distances in the current feature space, and no initialisation
trick will fix that.
}
}
