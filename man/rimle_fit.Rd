% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rimle_fit.R
\name{rimle_fit}
\alias{rimle_fit}
\title{Fit RIMLE Matrix-Variate Mixture Model}
\usage{
rimle_fit(
  x_list,
  g,
  k,
  pi_max = 0.5,
  gamma = 1000,
  max_iter = 100,
  tol = 1e-06,
  init = c("random", "hennig-coretto", "kmeans"),
  nstart = 10,
  q = 3,
  estimate_k = FALSE,
  k_grid = NULL,
  verbose = FALSE,
  use_parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{x_list}{A non-empty list of numeric matrices, each of the same size.}

\item{g}{Integer: number of Gaussian mixture components.}

\item{k}{Numeric: constant noise height (improper uniform density value).}

\item{pi_max}{Maximum proportion of observations assignable to noise
(default: 0.5).}

\item{gamma}{Eigenratio bound for covariance regularization (default: 1000).}

\item{max_iter}{Maximum ECM iterations (default: 100).}

\item{tol}{Convergence tolerance on the pseudo-log-likelihood trace.}

\item{init}{Initialization scheme: \code{"random"}, \code{"hennig-coretto"}, or \code{"kmeans"}.}

\item{nstart}{Number of independent random starts (default: 10).}

\item{q}{Nearest neighbor order for Hennig-Coretto initialization (default: 3).}

\item{estimate_k}{Logical: if TRUE, automatically select optimal k using
KS goodness-of-fit test.}

\item{k_grid}{Numeric vector: grid of k values for automatic selection.}

\item{verbose}{Logical: print iteration progress.}

\item{use_parallel}{Logical: run k-means restarts or k-grid search in parallel.}

\item{n_cores}{Integer: number of parallel workers (NULL = auto).}
}
\value{
A list of class \code{"rimle_fit"} containing fitted parameters,
posterior responsibilities, cluster assignments, and diagnostics.
}
\description{
Fits a robust improper mixture log-likelihood estimator (RIMLE) to a list
of matrix-valued observations. Supports automatic selection of the noise
constant k via a Kolmogorov-Smirnov goodness-of-fit test.
}
